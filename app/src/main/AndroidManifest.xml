<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.restaurant.project.mikuyapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".EntryMenuActivity"
            android:theme="@style/EntryMenuTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".signin.SignInActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".signup.SignUpActivity" />
        <activity android:name=".home.HomeActivity" />
        <activity android:name=".splash.SplashActivity" />
        <activity android:name=".detail.PlateDetailActivity" />
        <activity android:name=".reservation.make.MakeReservationActivity" />
    </application>

</manifest>