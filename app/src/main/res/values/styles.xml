<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="EntryMenuTheme" parent="AppTheme" />

    <style name="AppTheme.AddressLocation">
        <item name="colorAccent">@android:color/white</item>
    </style>

    <!--EntryMenuActivity  Style-->
    <style name="btnActionStyle">
        <item name="android:textAllCaps">true</item>
        <item name="android:layout_width">@dimen/widthBoxSignUp</item>
        <item name="android:layout_height">@dimen/heigthBoxSignUp</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">8dp</item>
        <item name="android:textSize">15sp</item>
        <item name="android:textColor">@color/colorWhite</item>
    </style>

    <style name="toolbarHomeStyle" parent="TextAppearance.AppCompat">
        <item name="android:textAllCaps">true</item>
    </style>
    <!--SignUp style -->
    <style name="edtRegisterStyle">
        <item name="android:background">@drawable/shape_edt_register</item>
        <item name="android:layout_width">@dimen/widthBoxSignUp</item>
        <item name="android:layout_height">@dimen/heigthBoxSignUp</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">8dp</item>
        <item name="android:textColor">@color/colorDark</item>
        <item name="android:textColorHint">@color/colorDisable</item>
        <item name="android:textSize">18sp</item>
    </style>

    <!-- RecyclerViewStyles-->
    <style name="recyclerScrollStyle">
        <item name="android:scrollbarAlwaysDrawVerticalTrack">true</item>
        <item name="android:scrollbarStyle">outsideOverlay</item>
        <item name="android:scrollbars">vertical</item>
        <item name="android:fadeScrollbars">true</item>
        <item name="android:scrollbarThumbVertical">@drawable/recycler_scrollbar</item>
        <item name="android:scrollbarTrackVertical">@android:color/transparent</item>
        <item name="android:scrollbarSize">2dp</item>
        <item name="android:scrollbarFadeDuration">2000</item>
        <item name="android:scrollbarDefaultDelayBeforeFade">1000</item>
    </style>
    <!--Radio button -->
    <style name="styleRadio">
        <item name="colorControlNormal">@color/colorDisable</item>
    </style>
</resources>
